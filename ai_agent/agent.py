"""Оркестрация агента: генерация рекомендаций.

Планируемая логика:
1. Собрать кандидатов (срок истекает ≤ 2 дня → скидка; высокий спрос/низкий
   остаток → наценка; остаток покрывает спрос < 3 дней → закупка);
2. Для каждого кандидата вызвать tools.py и получить готовые числа;
3. Отправить промпт (prompts.py) в LLM-модель (OPENAI-совместимый API,
   параметр LLM_MODEL — Qwen любого размера и т.п.);
4. Получить текстовое обоснование (reasoning) и вернуть структурированную
   рекомендацию с числами из tools (агент не подставляет собственные цифры);
5. Сохранение рекомендаций в price_recommendations (status = pending) через
   API backend.

Предоставляет функцию генерации отчёта для бота («топ-3 важных изменения»)
и pipeline утреннего запуска / команды /report.
"""