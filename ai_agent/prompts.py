"""Системный промпт и шаблоны сообщений для LLM.

Промпт формируется на основе agents.md:
- роль: советник владельца магазина;
- ЖЁСТКОЕ правило: НЕ производить арифметику, цену/маржу/объём брать только
  из вставленных результатов tools (get_stock_level, get_sales_trend,
  check_external_factors, calculate_reorder_amount);
- формат ответа: структурированный JSON (product_id, action: discount|markup|
  restock, recommended_price, reasoning на русском, 2–3 предложения);
- если данных не хватает — явно сообщить, не выдумывать.

Здесь хранятся: системный промпт, шаблон user-запроса для кандидата и
шаблон диагностики отсутствия данных.
"""