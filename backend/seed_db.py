"""Скрипт наполнения БД синтетическими данными (seed).

Запуск:  python -m backend.seed_db   (из корня репозитория)

Что делает (через services/synthetic_data.py и external_factors.py):
1. Создаёт схему таблиц (Base.metadata.create_all);
2. Заполняет товары, остатки, продажи за 3–6 месяцев с сезонностью;
3. Генерирует external_factors на исторический период + будущие 2 недели;
4. Создаёт историю цен (price_history) и пустую таблицу рекомендаций.

Безопасен для повторного запуска (пересоздание данных), поэтому скрипт
идемпотентен.
"""
