"""Черный/белый список авторизации (по ТЗ, раздел 5).

In-memory хранилище авторизованных user_id (set). Функции:
- is_authorized(user_id) -> bool — проверка членства в whitelist;
- authorize(user_id)      — добавить после успешного /auth;
- check_password(password) -> bool — сверка с ACCESS_KEY из окружения.
Whitelist живёт в памяти процесса бота (перезапуск сбрасывает —
повторная регистрация пользователем не требуется тестировщикам не делается).

Лог попыток несанкционированного доступа сохраняется для отчёта.
"""
