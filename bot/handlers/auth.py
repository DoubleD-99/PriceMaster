"""Хендлеры авторизации: /start и /auth [password].

- /start — приветствие и запрос секретного ключа (ACCESS_KEY);
- /auth [password] — сверяет пароль с ACCESS_KEY; при успехе добавляет
  user_id в whitelist (auth.authorize) с подтверждением для пользователя;
  при неудаче — сообщает об ошибке.
По ТЗ ключ передаётся преподавателю/тестировщикам отдельно.
"""