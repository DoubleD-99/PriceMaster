"""Обработка inline-кнопок рекомендаций.

- accept_<id>: POST-запрос в API — принять рекомендацию: цена товара
  обновляется, status -> accepted, запись в price_history (>recommendation_id);
- reject_<id>: status -> rejected (агент учтёт отказ в следующий раз);
- detail_<id>: ссылка на детальную аналитику/дашборд.

Подтверждения возвращаются пользователю сразу после действия.
"""