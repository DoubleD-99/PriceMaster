"""Inline-кнопки ответа к сообщениям агента (по ТЗ, раздел 5).

- «✅ Принять рекомендацию»          -> callback accept_<recommendation_id>:
   цена в БД обновляется, пишется price_history;
- «❌ Отклонить»                     -> callback reject_<recommendation_id>:
   статус rejected, агент учтёт отказ в следующий раз;
- «🔍 Подробнее»                     -> переход к детальной аналитике/дашборду.

Эталонные тексты кнопок — из ТЗ.
"""