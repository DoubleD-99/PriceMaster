"""Integration-тест сценария «бот → API → агент» (ТЗ раздел 6).

Цель — проверка потока данных, а не модели: LLM замокан (fixture mock_llm)
детерминированным ответом. Сценарий:
1. Бот получает команду /report от авторизованного user_id;
2. Бот запрашивает рекомендации у API (GET /recommendations);
3. API (при необходимости) запускает агента: tools.py дают числа, промпт
   уходит в mock_llm и возвращает фиксированное reasoning;
4. Ответ приходит в бота и рендерится списком «топ-3 важных изменения».

Дополнительно проверяется сквозной flow /auth → whitelist → блокировка
неавторизованного (заглушка из ТЗ).
"""
