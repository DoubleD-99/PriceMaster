"""Unit-тесты расчётных функций (ТЗ раздел 6).

Проверяют корректность МАТЕМАТИКИ в Python-функциях:
- calculate_margin(base_cost, current_price) — процент маржи (и деление на 0);
- calculate_discount_price(current_price, pct) — скидка не ниже себестоимости;
- calculate_reorder_amount — объём закупки с учётом спроса и остатка.

Это защищает правило agents.md «LLM не считает сам»: цифры, которые попали
в рекомендацию, генерируются именно этими функциями и только они тестируются.
"""
